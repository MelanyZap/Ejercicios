#include <stdio.h>
#include <stdlib.h>
#include "Persona.h"
/*struct Persona{;
char apellido[31];
char nombre[31];
int edad;
int isEmpty;
};*/
int main()
{
    ePersona per;

    per = alta(per);

    system("cls");

    mostrar(per);

    return 0;
}
